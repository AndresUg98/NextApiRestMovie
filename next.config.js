/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["play-lh.googleusercontent.com", "static.filmin.es"],
  },
};

module.exports = nextConfig;
